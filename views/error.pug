extends layout.pug

block content
    div(class="alert alert-secondary" role="alert") 
        h4 #{error}
        if additional_message
            p #{additional_message}
        if under_line
            hr
            p #{under_line}